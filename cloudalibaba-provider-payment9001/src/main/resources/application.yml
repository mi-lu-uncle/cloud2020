server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.0.104:8848 #配置Nacos地址：ip:port

management:
  endpoints:
    web:
      exposure:
        include: '*'


